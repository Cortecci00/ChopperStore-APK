import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.chopperstore.app',
  appName: 'ChopperStore',
  webDir: 'dist/angular18/browser'
};

export default config;
